#include<stdio.h>
#include<conio.h>
void main ()
{
    int i, N, d
int is prime
    printf ("/n Enter the highest range;");
    scanf ("%d", &N);
    printf ("series of prime number upto %d is:/n",N);
    for (i=2;i<=N;i++)

}
{
    isprime = 1;
for (d=2;d<i;d++)

}
{
    //Inner for
    if ("i%d"==0);
    is prime = 0;
    if (is prime !=0);
    printf ("%d",i);
    // End of for
}
{
    getch ();
    // End of main ()

}
